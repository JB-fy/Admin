// =================================================================================
// This is auto-generated by GoFrame CLI tool only once. Fill this file as you wish.
// =================================================================================

package dao

import (
	"{TplImportPrefix}/internal"
	"strings"

	"github.com/gogf/gf/v2/container/garray"
	"github.com/gogf/gf/v2/database/gdb"
)

// internal{TplTableNameCamelCase}Dao is internal type for wrapping internal DAO implements.
type internal{TplTableNameCamelCase}Dao = *internal.{TplTableNameCamelCase}Dao

// {TplTableNameCamelLowerCase}Dao is the data access object for table {TplTableName}.
// You can define custom methods on it to extend its functionality as you wish.
type {TplTableNameCamelLowerCase}Dao struct {
	internal{TplTableNameCamelCase}Dao
}

var (
	// {TplTableNameCamelCase} is globally public accessible object for table {TplTableName} operations.
	{TplTableNameCamelCase} = {TplTableNameCamelLowerCase}Dao{
		internal.New{TplTableNameCamelCase}Dao(),
	}
)

// 解析insert
func (dao{TplTableNameCamelCase} *{TplTableNameCamelLowerCase}Dao) ParseInsert(insert []map[string]interface{}, fill ...bool) gdb.ModelHandler {
	return func(m *gdb.Model) *gdb.Model {
		insertData := make([]map[string]interface{}, len(insert))
		for index, item := range insert {
			insertData[index] = map[string]interface{}{}
			for k, v := range item {
				switch k {
				case "id":
					insertData[index][dao{TplTableNameCamelCase}.PrimaryKey()] = v
				default:
					//数据库不存在的字段过滤掉，未传值默认true
					if (len(fill) == 0 || fill[0]) && !dao{TplTableNameCamelCase}.ColumnArrG().Contains(k) {
						continue
					}
					insertData[index][k] = v
				}
			}
		}
		if len(insertData) == 1 {
			m = m.Data(insertData[0])
		} else {
			m = m.Data(insertData)
		}
		return m
	}
}

// 解析update
func (dao{TplTableNameCamelCase} *{TplTableNameCamelLowerCase}Dao) ParseUpdate(update map[string]interface{}, fill ...bool) gdb.ModelHandler {
	return func(m *gdb.Model) *gdb.Model {
		updateData := map[string]interface{}{}
		for k, v := range update {
			switch k {
			case "id":
				updateData[dao{TplTableNameCamelCase}.Table()+"."+dao{TplTableNameCamelCase}.PrimaryKey()] = v
			default:
				//数据库不存在的字段过滤掉，未传值默认true
				if (len(fill) == 0 || fill[0]) && !dao{TplTableNameCamelCase}.ColumnArrG().Contains(k) {
					continue
				}
				updateData[dao{TplTableNameCamelCase}.Table()+"."+k] = v
			}
		}
		//m = m.Data(updateData) //字段被解析成`table.xxxx`，正确的应该是`table`.`xxxx`
		//解决字段被解析成`table.xxxx`的BUG
		fieldArr := []string{}
		valueArr := []interface{}{}
		for k, v := range updateData {
			fieldArr = append(fieldArr, k+" = ?")
			valueArr = append(valueArr, v)
		}
		data := []interface{}{strings.Join(fieldArr, ",")}
		data = append(data, valueArr...)
		m = m.Data(data...)
		return m
	}
}

// 解析field
func (dao{TplTableNameCamelCase} *{TplTableNameCamelLowerCase}Dao) ParseField(field []string, joinTableArr *[]string) gdb.ModelHandler {
	return func(m *gdb.Model) *gdb.Model {
		afterField := []string{}
		for _, v := range field {
			switch v {
			/* case "xxxx":
			m = dao{TplTableNameCamelCase}.ParseJoin("xxxx", joinTableArr)(m)
			afterField = append(afterField, v) */
			case "id":
				m = m.Fields(dao{TplTableNameCamelCase}.Table() + "." + dao{TplTableNameCamelCase}.PrimaryKey() + " AS " + v)
			default:
				if dao{TplTableNameCamelCase}.ColumnArrG().Contains(v) {
					m = m.Fields(dao{TplTableNameCamelCase}.Table() + "." + v)
				} else {
					m = m.Fields(v)
				}
			}
		}
		if len(afterField) > 0 {
			m = m.Hook(dao{TplTableNameCamelCase}.AfterField(afterField))
		}
		return m
	}
}

// 解析filter
func (dao{TplTableNameCamelCase} *{TplTableNameCamelLowerCase}Dao) ParseFilter(filter map[string]interface{}, joinTableArr *[]string) gdb.ModelHandler {
	return func(m *gdb.Model) *gdb.Model {
		for k, v := range filter {
			switch k {
			case "id", "idArr":
				m = m.Where(dao{TplTableNameCamelCase}.Table()+"."+dao{TplTableNameCamelCase}.PrimaryKey(), v)
			case "excId":
				m = m.WhereNot(dao{TplTableNameCamelCase}.Table()+"."+dao{TplTableNameCamelCase}.PrimaryKey(), v)
			case "excIdArr":
				m = m.WhereNotIn(dao{TplTableNameCamelCase}.Table()+"."+dao{TplTableNameCamelCase}.PrimaryKey(), v)
			case "startTime":
				m = m.WhereGTE(dao{TplTableNameCamelCase}.Table()+".createTime", v)
			case "endTime":
				m = m.WhereLTE(dao{TplTableNameCamelCase}.Table()+".createTime", v)
			case "keyword":
				keywordField := strings.ReplaceAll(dao{TplTableNameCamelCase}.PrimaryKey(), "Id", "Name")
				switch v := v.(type) {
				case *string:
					m = m.WhereLike(dao{TplTableNameCamelCase}.Table()+"."+keywordField, *v)
				case string:
					m = m.WhereLike(dao{TplTableNameCamelCase}.Table()+"."+keywordField, v)
				default:
					m = m.Where(dao{TplTableNameCamelCase}.Table()+"."+keywordField, v)
				}
			default:
				kArr := strings.Split(k, " ")
				if dao{TplTableNameCamelCase}.ColumnArrG().Contains(kArr[0]) {
					m = m.Where(dao{TplTableNameCamelCase}.Table()+"."+k, v)
				} else {
					m = m.Where(k, v)
				}
			}
		}
		return m
	}
}

// 解析group
func (dao{TplTableNameCamelCase} *{TplTableNameCamelLowerCase}Dao) ParseGroup(group []string, joinTableArr *[]string) gdb.ModelHandler {
	return func(m *gdb.Model) *gdb.Model {
		for _, v := range group {
			switch v {
			case "id":
				m = m.Group(dao{TplTableNameCamelCase}.Table() + "." + dao{TplTableNameCamelCase}.PrimaryKey())
			default:
				if dao{TplTableNameCamelCase}.ColumnArrG().Contains(v) {
					m = m.Group(dao{TplTableNameCamelCase}.Table() + "." + v)
				} else {
					m = m.Group(v)
				}
			}
		}
		return m
	}
}

// 解析order
func (dao{TplTableNameCamelCase} *{TplTableNameCamelLowerCase}Dao) ParseOrder(order [][2]string, joinTableArr *[]string) func(m *gdb.Model) *gdb.Model {
	return func(m *gdb.Model) *gdb.Model {
		for _, v := range order {
			switch v[0] {
			case "id":
				m = m.Order(dao{TplTableNameCamelCase}.Table()+"."+dao{TplTableNameCamelCase}.PrimaryKey(), v[1])
			default:
				if dao{TplTableNameCamelCase}.ColumnArrG().Contains(v[0]) {
					m = m.Order(dao{TplTableNameCamelCase}.Table()+"."+v[0], v[1])
				} else {
					m = m.Order(v[0], v[1])
				}
			}
		}
		return m
	}
}

// 解析join
func (dao{TplTableNameCamelCase} *{TplTableNameCamelLowerCase}Dao) ParseJoin(joinCode string, joinTableArr *[]string) func(m *gdb.Model) *gdb.Model {
	return func(m *gdb.Model) *gdb.Model {
		switch joinCode {
		/* case "xxxx":
		xxxxTable := xxxx.Table()
		if !garray.NewStrArrayFrom(*joinTableArr).Contains(xxxxTable) {
			*joinTableArr = append(*joinTableArr, xxxxTable)
			m = m.LeftJoin(xxxxTable, xxxxTable+"."+dao{TplTableNameCamelCase}.PrimaryKey()+" = "+dao{TplTableNameCamelCase}.Table()+"."+dao{TplTableNameCamelCase}.PrimaryKey())
		} */
		}
		return m
	}
}

// 获取数据后，再处理的字段
func (dao{TplTableNameCamelCase} *{TplTableNameCamelLowerCase}Dao) AfterField(afterField []string) gdb.HookHandler {
	return gdb.HookHandler{
		Select: func(ctx context.Context, in *gdb.HookSelectInput) (result gdb.Result, err error) {
			result, err = in.Next(ctx)
			if err != nil {
				return
			}
			for i, record := range result {
				for _, v := range afterField {
					switch v {
					/* case "xxxx":
					record[v] = gvar.New("") */
					}
				}
				result[i] = record
			}
			return
		},
	}
}

// 详情
func (dao{TplTableNameCamelCase} *{TplTableNameCamelLowerCase}Dao) Info(ctx context.Context, filter map[string]interface{}, field []string, order ...[2]string) (info gdb.Record, err error) {
	joinTableArr := []string{}
	model := dao{TplTableNameCamelCase}.Ctx(ctx)
	if len(field) > 0 {
		model = model.Handler(dao{TplTableNameCamelCase}.ParseField(field, &joinTableArr))
	}
	if len(filter) > 0 {
		model = model.Handler(dao{TplTableNameCamelCase}.ParseFilter(filter, &joinTableArr))
	}
	if len(order) > 0 {
		model = model.Handler(dao{TplTableNameCamelCase}.ParseOrder(order, &joinTableArr))
	}
	info, err = model.One()
	return
}

// 列表
func (dao{TplTableNameCamelCase} *{TplTableNameCamelLowerCase}Dao) List(ctx context.Context, filter map[string]interface{}, field []string, order ...[2]string) (list gdb.Result, err error) {
	joinTableArr := []string{}
	model := dao{TplTableNameCamelCase}.Ctx(ctx)
	if len(field) > 0 {
		model = model.Handler(dao{TplTableNameCamelCase}.ParseField(field, &joinTableArr))
	}
	if len(filter) > 0 {
		model = model.Handler(dao{TplTableNameCamelCase}.ParseFilter(filter, &joinTableArr))
	}
	if len(order) > 0 {
		model = model.Handler(dao{TplTableNameCamelCase}.ParseOrder(order, &joinTableArr))
	}
	list, err = model.All()
	return
}

// Fill with you ideas below.