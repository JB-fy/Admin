package logic

import (
	daoAuth "api/internal/model/dao/auth"
	"api/internal/service"
	"context"

	"github.com/gogf/gf/v2/database/gdb"
)

type s{TplTableNameCamelCase} struct{}

func New{TplTableNameCamelCase}() *s{TplTableNameCamelCase} {
	return &s{TplTableNameCamelCase}{}
}

func init() {
	service.Register{TplTableNameCamelCase}(New{TplTableNameCamelCase}())
}

// 总数
func (logic *s{TplTableNameCamelCase}) Count(ctx context.Context, filter map[string]interface{}) (count int, err error) {
	dao{TplTableNameCamelCase} := daoAuth.{TplTableNameCamelCase}
	joinCodeArr := []string{}
	model := dao{TplTableNameCamelCase}.Ctx(ctx)
	if len(filter) > 0 {
		model = model.Handler(dao{TplTableNameCamelCase}.ParseFilter(filter, &joinCodeArr))
	}
	if len(joinCodeArr) > 0 {
		count, err = model.Handler(dao{TplTableNameCamelCase}.ParseGroup([]string{"id"}, &joinCodeArr)).Distinct().Count(dao{TplTableNameCamelCase}.PrimaryKey())
	} else {
		count, err = model.Count()
	}
	return
}

// 列表
func (logic *s{TplTableNameCamelCase}) List(ctx context.Context, filter map[string]interface{}, field []string, order [][2]string, offset int, limit int) (list gdb.Result, err error) {
	dao{TplTableNameCamelCase} := daoAuth.{TplTableNameCamelCase}
	joinCodeArr := []string{}
	model := dao{TplTableNameCamelCase}.Ctx(ctx)
	if len(field) > 0 {
		model = model.Handler(dao{TplTableNameCamelCase}.ParseField(field, &joinCodeArr))
	}
	if len(filter) > 0 {
		model = model.Handler(dao{TplTableNameCamelCase}.ParseFilter(filter, &joinCodeArr))
	}
	if len(order) > 0 {
		model = model.Handler(dao{TplTableNameCamelCase}.ParseOrder(order, &joinCodeArr))
	}
	if len(joinCodeArr) > 0 {
		model = model.Handler(dao{TplTableNameCamelCase}.ParseGroup([]string{"id"}, &joinCodeArr))
	}
	if limit > 0 {
		model = model.Offset(offset).Limit(limit)
	}
	list, err = model.All()
	return
}

// 详情
func (logic *s{TplTableNameCamelCase}) Info(ctx context.Context, filter map[string]interface{}, field []string, order [][2]string) (info gdb.Record, err error) {
	dao{TplTableNameCamelCase} := daoAuth.{TplTableNameCamelCase}
	joinCodeArr := []string{}
	model := dao{TplTableNameCamelCase}.Ctx(ctx)
	if len(field) > 0 {
		model = model.Handler(dao{TplTableNameCamelCase}.ParseField(field, &joinCodeArr))
	}
	if len(filter) > 0 {
		model = model.Handler(dao{TplTableNameCamelCase}.ParseFilter(filter, &joinCodeArr))
	}
	if len(order) > 0 {
		model = model.Handler(dao{TplTableNameCamelCase}.ParseOrder(order, &joinCodeArr))
	}
	if len(joinCodeArr) > 0 {
		model = model.Handler(dao{TplTableNameCamelCase}.ParseGroup([]string{"id"}, &joinCodeArr))
	}
	info, err = model.One()
	return
}

// 创建
func (logic *s{TplTableNameCamelCase}) Create(ctx context.Context, data []map[string]interface{}) (id int64, err error) {
	dao{TplTableNameCamelCase} := daoAuth.{TplTableNameCamelCase}
	model := dao{TplTableNameCamelCase}.Ctx(ctx)
	if len(data) > 0 {
		model = model.Handler(dao{TplTableNameCamelCase}.ParseInsert(data))
	}
	if len(data) == 1 {
		id, err = model.InsertAndGetId()
		return
	}
	result, err := model.Insert()
	if err != nil {
		return
	}
	id, err = result.RowsAffected()
	return
}

// 更新
func (logic *s{TplTableNameCamelCase}) Update(ctx context.Context, data map[string]interface{}, filter map[string]interface{}, order [][2]string, offset int, limit int) (row int64, err error) {
	dao{TplTableNameCamelCase} := daoAuth.{TplTableNameCamelCase}
	joinCodeArr := []string{}
	model := dao{TplTableNameCamelCase}.Ctx(ctx)
	if len(data) > 0 {
		model = model.Handler(dao{TplTableNameCamelCase}.ParseUpdate(data))
	}
	if len(filter) > 0 {
		model = model.Handler(dao{TplTableNameCamelCase}.ParseFilter(filter, &joinCodeArr))
	}
	if len(order) > 0 {
		model = model.Handler(dao{TplTableNameCamelCase}.ParseOrder(order, &joinCodeArr))
	}
	if limit > 0 {
		model = model.Offset(offset).Limit(limit)
	}
	result, err := model.Update()
	if err != nil {
		return
	}
	row, err = result.RowsAffected()
	return
}

// 删除
func (logic *s{TplTableNameCamelCase}) Delete(ctx context.Context, filter map[string]interface{}, order [][2]string, offset int, limit int) (row int64, err error) {
	dao{TplTableNameCamelCase} := daoAuth.{TplTableNameCamelCase}
	joinCodeArr := []string{}
	model := dao{TplTableNameCamelCase}.Ctx(ctx)
	if len(filter) > 0 {
		model = model.Handler(dao{TplTableNameCamelCase}.ParseFilter(filter, &joinCodeArr))
	}
	if len(order) > 0 {
		model = model.Handler(dao{TplTableNameCamelCase}.ParseOrder(order, &joinCodeArr))
	}
	if limit > 0 {
		model = model.Offset(offset).Limit(limit)
	}
	result, err := model.Delete()
	if err != nil {
		return
	}
	row, err = result.RowsAffected()
	return
}
