// =================================================================================
// This is auto-generated by GoFrame CLI tool only once. Fill this file as you wish.
// =================================================================================

package dao

import (
	daoAuth "api/internal/model/dao/auth"
	"api/internal/model/dao/platform/internal"
	"context"
	"strings"

	"github.com/gogf/gf/v2/container/garray"
	"github.com/gogf/gf/v2/container/gvar"
	"github.com/gogf/gf/v2/database/gdb"
	"github.com/gogf/gf/v2/frame/g"
)

// internalAdminDao is internal type for wrapping internal DAO implements.
type internalAdminDao = *internal.AdminDao

// adminDao is the data access object for table platform_admin.
// You can define custom methods on it to extend its functionality as you wish.
type adminDao struct {
	internalAdminDao
}

var (
	// Admin is globally public accessible object for table platform_admin operations.
	Admin = adminDao{
		internal.NewAdminDao(),
	}
)

// 解析insert
func (daoAdmin *adminDao) ParseInsert(insert []map[string]interface{}, fill ...bool) gdb.ModelHandler {
	return func(m *gdb.Model) *gdb.Model {
		insertData := make([]map[string]interface{}, len(insert))
		for index, item := range insert {
			insertData[index] = map[string]interface{}{}
			for k, v := range item {
				switch k {
				case "id":
					insertData[index][daoAdmin.PrimaryKey()] = v
				default:
					//数据库不存在的字段过滤掉，未传值默认true
					if (len(fill) == 0 || fill[0]) && !daoAdmin.ColumnArrG().Contains(k) {
						continue
					}
					insertData[index][k] = v
				}
			}
		}
		if len(insertData) == 1 {
			m = m.Data(insertData[0])
		} else {
			m = m.Data(insertData)
		}
		return m
	}
}

// 解析update
func (daoAdmin *adminDao) ParseUpdate(update map[string]interface{}, fill ...bool) gdb.ModelHandler {
	return func(m *gdb.Model) *gdb.Model {
		updateData := map[string]interface{}{}
		for k, v := range update {
			switch k {
			case "id":
				updateData[daoAdmin.Table()+"."+daoAdmin.PrimaryKey()] = v
			default:
				//数据库不存在的字段过滤掉，未传值默认true
				if (len(fill) == 0 || fill[0]) && !daoAdmin.ColumnArrG().Contains(k) {
					continue
				}
				updateData[daoAdmin.Table()+"."+k] = v
			}
		}
		//m = m.Data(updateData) //字段被解析成`table.xxxx`，正确的应该是`table`.`xxxx`
		//解决字段被解析成`table.xxxx`的BUG
		fieldArr := []string{}
		valueArr := []interface{}{}
		for k, v := range updateData {
			fieldArr = append(fieldArr, k+" = ?")
			valueArr = append(valueArr, v)
		}
		data := []interface{}{strings.Join(fieldArr, ",")}
		data = append(data, valueArr...)
		m = m.Data(data...)
		return m
	}
}

// 解析field
func (daoAdmin *adminDao) ParseField(field []string, joinTableArr *[]string) gdb.ModelHandler {
	return func(m *gdb.Model) *gdb.Model {
		afterField := []string{}
		for _, v := range field {
			switch v {
			/* case "xxxx":
			m = daoAdmin.ParseJoin("xxxx", joinTableArr)(m)
			afterField = append(afterField, v) */
			case "id":
				m = m.Fields(daoAdmin.Table() + "." + daoAdmin.PrimaryKey() + " AS " + v)
			case "roleIdArr":
				//需要id字段
				m = m.Fields(daoAdmin.Table() + "." + daoAdmin.PrimaryKey())
				afterField = append(afterField, v)
			default:
				if daoAdmin.ColumnArrG().Contains(v) {
					m = m.Fields(daoAdmin.Table() + "." + v)
				} else {
					m = m.Fields(v)
				}
			}
		}
		if len(afterField) > 0 {
			m = m.Hook(daoAdmin.AfterField(afterField))
		}
		return m
	}
}

// 解析filter
func (daoAdmin *adminDao) ParseFilter(filter map[string]interface{}, joinTableArr *[]string) gdb.ModelHandler {
	return func(m *gdb.Model) *gdb.Model {
		for k, v := range filter {
			switch k {
			case "id", "idArr":
				m = m.Where(daoAdmin.Table()+"."+daoAdmin.PrimaryKey(), v)
			case "excId":
				m = m.WhereNot(daoAdmin.Table()+"."+daoAdmin.PrimaryKey(), v)
			case "excIdArr":
				m = m.WhereNotIn(daoAdmin.Table()+"."+daoAdmin.PrimaryKey(), v)
			case "startTime":
				m = m.WhereGTE(daoAdmin.Table()+".createTime", v)
			case "endTime":
				m = m.WhereLTE(daoAdmin.Table()+".createTime", v)
			case "keyword":
				keywordField := strings.ReplaceAll(daoAdmin.PrimaryKey(), "Id", "Name")
				switch v := v.(type) {
				case *string:
					m = m.WhereLike(daoAdmin.Table()+"."+keywordField, *v)
				case string:
					m = m.WhereLike(daoAdmin.Table()+"."+keywordField, v)
				default:
					m = m.Where(daoAdmin.Table()+"."+keywordField, v)
				}
			case "accountOrPhone":
				if g.Validator().Rules("required|integer").Data(v).Run(m.GetCtx()) == nil {
					m = m.Where(daoAdmin.Table()+".phone", v)
				} else {
					m = m.Where(daoAdmin.Table()+".account", v)
				}
			case "roleId":
				m = m.Where(daoAuth.RoleRelOfPlatformAdmin.Table()+"."+k, v)
				m = daoAdmin.ParseJoin("roleRelOfPlatformAdmin", joinTableArr)(m)
			default:
				kArr := strings.Split(k, " ")
				if daoAdmin.ColumnArrG().Contains(kArr[0]) {
					m = m.Where(daoAdmin.Table()+"."+k, v)
				} else {
					m = m.Where(k, v)
				}
			}
		}
		return m
	}
}

// 解析group
func (daoAdmin *adminDao) ParseGroup(group []string, joinTableArr *[]string) gdb.ModelHandler {
	return func(m *gdb.Model) *gdb.Model {
		for _, v := range group {
			switch v {
			case "id":
				m = m.Group(daoAdmin.Table() + "." + daoAdmin.PrimaryKey())
			default:
				if daoAdmin.ColumnArrG().Contains(v) {
					m = m.Group(daoAdmin.Table() + "." + v)
				} else {
					m = m.Group(v)
				}
			}
		}
		return m
	}
}

// 解析order
func (daoAdmin *adminDao) ParseOrder(order [][2]string, joinTableArr *[]string) func(m *gdb.Model) *gdb.Model {
	return func(m *gdb.Model) *gdb.Model {
		for _, v := range order {
			switch v[0] {
			case "id":
				m = m.Order(daoAdmin.Table()+"."+daoAdmin.PrimaryKey(), v[1])
			default:
				if daoAdmin.ColumnArrG().Contains(v[0]) {
					m = m.Order(daoAdmin.Table()+"."+v[0], v[1])
				} else {
					m = m.Order(v[0], v[1])
				}
			}
		}
		return m
	}
}

// 解析join
func (daoAdmin *adminDao) ParseJoin(joinCode string, joinTableArr *[]string) func(m *gdb.Model) *gdb.Model {
	return func(m *gdb.Model) *gdb.Model {
		switch joinCode {
		/* case "xxxx":
		xxxxTable := xxxx.Table()
		if !garray.NewStrArrayFrom(*joinTableArr).Contains(xxxxTable) {
			*joinTableArr = append(*joinTableArr, xxxxTable)
			m = m.LeftJoin(xxxxTable, xxxxTable+"."+daoAdmin.PrimaryKey()+" = "+daoAdmin.Table()+"."+daoAdmin.PrimaryKey())
		} */
		case "roleRelOfPlatformAdmin":
			roleRelOfPlatformAdminTable := daoAuth.RoleRelOfPlatformAdmin.Table()
			if !garray.NewStrArrayFrom(*joinTableArr).Contains(roleRelOfPlatformAdminTable) {
				*joinTableArr = append(*joinTableArr, roleRelOfPlatformAdminTable)
				m = m.LeftJoin(roleRelOfPlatformAdminTable, roleRelOfPlatformAdminTable+"."+daoAdmin.PrimaryKey()+" = "+daoAdmin.Table()+"."+daoAdmin.PrimaryKey())
			}
		}
		return m
	}
}

// 获取数据后，再处理的字段
func (daoAdmin *adminDao) AfterField(afterField []string) gdb.HookHandler {
	return gdb.HookHandler{
		Select: func(ctx context.Context, in *gdb.HookSelectInput) (result gdb.Result, err error) {
			result, err = in.Next(ctx)
			if err != nil {
				return
			}
			for i, record := range result {
				for _, v := range afterField {
					switch v {
					/* case "xxxx":
					record[v] = gvar.New("") */
					case "roleIdArr":
						idArr, _ := daoAuth.RoleRelOfPlatformAdmin.Ctx(ctx).Where("adminId", record[daoAdmin.PrimaryKey()]).Fields("roleId").Array()
						record[v] = gvar.New(idArr)
					}
				}
				result[i] = record
			}
			return
		},
	}
}

// 详情
func (daoAdmin *adminDao) Info(ctx context.Context, filter map[string]interface{}, field []string, order ...[2]string) (info gdb.Record, err error) {
	joinTableArr := []string{}
	model := daoAdmin.Ctx(ctx)
	if len(field) > 0 {
		model = model.Handler(daoAdmin.ParseField(field, &joinTableArr))
	}
	if len(filter) > 0 {
		model = model.Handler(daoAdmin.ParseFilter(filter, &joinTableArr))
	}
	if len(order) > 0 {
		model = model.Handler(daoAdmin.ParseOrder(order, &joinTableArr))
	}
	info, err = model.One()
	return
}

// 列表
func (daoAdmin *adminDao) List(ctx context.Context, filter map[string]interface{}, field []string, order ...[2]string) (list gdb.Result, err error) {
	joinTableArr := []string{}
	model := daoAdmin.Ctx(ctx)
	if len(field) > 0 {
		model = model.Handler(daoAdmin.ParseField(field, &joinTableArr))
	}
	if len(filter) > 0 {
		model = model.Handler(daoAdmin.ParseFilter(filter, &joinTableArr))
	}
	if len(order) > 0 {
		model = model.Handler(daoAdmin.ParseOrder(order, &joinTableArr))
	}
	list, err = model.All()
	return
}

// Fill with you ideas below.
